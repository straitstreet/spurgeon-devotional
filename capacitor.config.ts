import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.straitstreet.spurgeon',
  appName: 'Spurgeon Devotional',
  webDir: 'dist'
};

export default config;
